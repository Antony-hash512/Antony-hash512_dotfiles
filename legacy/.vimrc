syntax on
set number
set list
set listchars=tab:»\ »,space:·
set clipboard=unnamedplus

" Настройки табуляции
set tabstop=4        " Отображать табуляцию как 4 пробела
set softtabstop=0    " Отключить замену табов на пробелы при редактировании
set shiftwidth=4     " Ширина отступа для команд (>> и <<)
set noexpandtab      " Использовать реальные табуляции (\t), а не пробелы


" call plug#begin('~/.vim/plugged')

" Plug 'github/copilot.vim'

" call plug#end()
